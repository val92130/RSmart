using System;
using Microsoft.SPOT;

namespace RSmartControl
{
    public class Box
    {
        private double x, y;
        public Box(double x, double y)
        {
            this.x = x;
            this.y = y;
        }
    }
}
